import 'package:flutter/material.dart';

class FormatsScreen extends StatelessWidget {
  const FormatsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('FormatsScreen'));
  }
}
